fastapi
uvicorn
asyncpg
pydantic
pydantic-settings
python-dotenv
openai

# 예약 취소 예측 모델(TF SavedModel) 서빙용
# struct2tensor 0.52.0 이상은 wheel 자체가 깨져 있어(.so 누락) 절대 쓰면 안 됨.
# tensorflow==2.16.2는 설치는 되지만 BoostedTrees 커널이 없어서(No OpKernel was registered) 모델 로드가 실패함.
# 2.15.x로 한 단계 낮춰서 재시도.
tensorflow==2.15.0
struct2tensor==0.46.0
tensorflow-metadata>=1.15.0,<1.16.0
